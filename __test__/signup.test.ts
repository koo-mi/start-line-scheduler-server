// Come back to this later

// import supertest from "supertest";
// import app from "../utils/app";
// const { registerUser } = require('../controllers/signup-controller');

// describe("Test Signup Request", () => {
//     describe("Should give 400 when", () => {
//         test('when username already exist', async () => {
//             const response = await supertest(app).post("/signup")
//         });
//     })
// })